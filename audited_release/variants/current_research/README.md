# Later research source

These files record the current post-controlled-run model and loss development. They are included to preserve the project history, but they are not the source used to create the IVCNZ and PCM epoch-28 checkpoints. Use `../../reported_controlled_main/` for those reported tables.
